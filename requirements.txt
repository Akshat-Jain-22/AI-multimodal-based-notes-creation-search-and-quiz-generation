# --- Core pipeline ---
# IMPORTANT: install CPU-only torch FIRST, before this file, or pip will pull
# the full CUDA build as a sentence-transformers dependency (multi-GB
# download, useless on this CPU-only 8GB RAM setup):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
faster-whisper==1.0.3
groq==0.11.0
python-dotenv==1.0.1

# --- LangChain / RAG ---
langchain==0.3.7
langchain-core==0.3.15
langchain-groq==0.2.1
langchain-huggingface==0.1.2
langchain-chroma==0.1.4
chromadb==0.5.20
sentence-transformers==3.2.1

# --- Backend / API ---
fastapi==0.115.5
uvicorn[standard]==0.32.0
python-multipart==0.0.17
pydantic==2.9.2

# --- Audio/video/OCR/slides ---
opencv-python==4.10.0.84
pytesseract==0.3.13
Pillow==10.4.0
numpy==1.26.4
python-pptx==1.0.2
pdf2image==1.17.0

# --- Diagram/chart rendering ---
matplotlib==3.9.2

# --- Testing (dev only, not needed in production) ---
httpx==0.27.2