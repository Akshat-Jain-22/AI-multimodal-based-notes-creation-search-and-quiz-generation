# New backend-only deps — your existing venv already has faster-whisper,
# groq, langchain*, chromadb, sentence-transformers, etc. working, so this
# skips reinstalling/rebuilding any of that (which is what triggered the
# `av` build-from-source failure on Python 3.13).
fastapi==0.115.5
uvicorn[standard]==0.32.0
python-multipart==0.0.17
httpx==0.27.2